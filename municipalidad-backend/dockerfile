FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY --chown=node:node . .

RUN mkdir -p /app/uploads/documentos \
  && chown -R node:node /app/uploads

EXPOSE 3000

USER node

CMD ["npm", "start"]
